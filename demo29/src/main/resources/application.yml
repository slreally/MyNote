spring:
  datasource:
    url: jdbc:mysql://localhost:3306/mytest?useUnicode=true&characterEncoding=UTF-8
    username: root
    password: Qianrui1994
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    show-sql: true  #在控制台可以查看hibernate发送的sql语句
    properties:
      hibernate:
        format_sql: true  #格式化sql
    open-in-view: false